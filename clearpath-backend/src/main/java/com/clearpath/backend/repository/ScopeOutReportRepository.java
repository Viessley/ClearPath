package com.clearpath.backend.repository;

import com.clearpath.backend.entity.ScopeOutReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScopeOutReportRepository extends JpaRepository<ScopeOutReport, String> {
}
